import { TDefaultOption } from './TDefaultOption';

export type TDefaultOptions = {
  bundler: TDefaultOption;
  nodePort: TDefaultOption;
  clientConfig: TDefaultOption;
  clientPort: TDefaultOption;
  serverConfig: TDefaultOption;
  serverPort: TDefaultOption;
}
